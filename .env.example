# ArbiLink Environment Variables
# Copy to .env and fill in real values (never commit .env!)

# Required for deployment and the relayer bot
PRIVATE_KEY=0x...                           # Deployer/owner private key
                                            # Also used as the relayer wallet by default
INFURA_KEY=...                              # Infura project ID — used by the relayer for Arbitrum Sepolia RPC

# Optional: private key of the hub signing key that signs execution proofs.
# Defaults to PRIVATE_KEY when not set (i.e. deployer == signing key).
# The corresponding address must be set as hubSigningKey in each Receiver.
HUB_SIGNING_KEY=0x...

# API keys for block explorer verification
ARBISCAN_API_KEY=...
ETHERSCAN_API_KEY=...
BASESCAN_API_KEY=...
