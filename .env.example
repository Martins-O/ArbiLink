# ArbiLink Environment Variables
# Copy to .env and fill in real values (never commit .env!)

# Required for deployment
PRIVATE_KEY=0x...                           # Deployer/owner private key
INFURA_KEY=...                              # Infura project ID

# Optional: address of the off-chain key that signs execution proofs
# Defaults to the deployer address if not set
HUB_SIGNING_KEY=0x...

# API keys for block explorer verification
ARBISCAN_API_KEY=...
ETHERSCAN_API_KEY=...
BASESCAN_API_KEY=...
